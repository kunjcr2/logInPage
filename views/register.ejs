<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log-In Page</title>
    <style>
        body {
            font-family: "Open Sans", sans-serif;
            background-color: #f9f9f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
        }

        form {
            display: flex;
            flex-direction: column;
            position: relative;
        }

        .form__group {
            position: relative;
            margin-bottom: 20px;
        }

        .form__label {
    position: absolute;
    top: 10px;  /* Adjusted slightly for spacing */
    left: 5px;
    font-size: 1rem;
    color: #aaa;
    transition: all 0.2s ease-in-out;
}

.form__field:focus + .form__label,
.form__field:not(:placeholder-shown) + .form__label {
    top: -10px; /* Moves label higher on focus or text entry */
    font-size: 0.85rem;
    color: #2196F3;  /* Set your desired color on focus */
}

.form__field {
    font-family: inherit;
    font-size: 1rem;
    padding: 10px 5px;
    margin-bottom: 20px; /* Space between input fields */
    border: none;
    border-bottom: 2px solid #ccc;
    outline: none;
    transition: border-color 0.3s ease;
    background-color: transparent;
    width: 100%;
}

.form__field:focus {
    border-bottom: 2px solid #2196F3;
}


        button {
            font-family: "Open Sans", sans-serif;
            font-size: 16px;
            letter-spacing: 2px;
            text-decoration: none;
            text-transform: uppercase;
            color: #000;
            cursor: pointer;
            border: 3px solid;
            padding: 0.25em 0.5em;
            box-shadow:
                1px 1px 0px 0px,
                2px 2px 0px 0px,
                3px 3px 0px 0px,
                4px 4px 0px 0px,
                5px 5px 0px 0px;
            position: relative;
            user-select: none;
            -webkit-user-select: none;
            touch-action: manipulation;
            background-color: #fff;
            width: 100%;
        }

        button:active {
            box-shadow: 0px 0px 0px 0px;
            top: 5px;
            left: 5px;
        }

        button:hover {
            background: #38ef7d;
            /* Modern green on hover */
        }

        @media (min-width: 768px) {
            button {
                padding: 0.25em 0.75em;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <form action="/register" method="post">
            <div class="form__group">
                <input type="text" class="form__field" name="name" id="name" placeholder="Enter your Full name"
                    required />
                <label for="name" class="form__label">Enter your Full name</label>
            </div>
            <div class="form__group">
                <input type="text" class="form__field" name="age" id="age" placeholder="Enter your Age" required />
                <label for="age" class="form__label">Enter your Age</label>
            </div>
            <div class="form__group">
                <input type="text" class="form__field" name="email" id="email" placeholder="Enter your email"
                    required />
                <label for="email" class="form__label">Enter your email</label>
            </div>
            <div class="form__group">
                <input type="text" class="form__field" name="password" id="password" placeholder="Enter your password"
                    required />
                <label for="password" class="form__label">Enter your password</label>
            </div>
            <div class="form__group">
                <input type="text" class="form__field" name="passwordConf" id="passwordConf"
                    placeholder="Confirm your Password" required />
                <label for="passwordConf" class="form__label">Confirm your Password</label>
            </div>
            <button type="submit">Register</button>
        </form>
    </div>
</body>

</html>